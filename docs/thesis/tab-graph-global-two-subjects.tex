% docs/thesis/tab-graph-global-two-subjects.tex
% Derived 2026-08-09 | apd 187 materials / 1193 chunks (corpus_hash cf0c85b4...)
%                    | passc 15 materials / 341 chunks (corpus_hash b0b54cfa...)
% Graphs: both openai/gpt-oss-120b, graph.gleanings=0, graph.context_window=16384, entity_types
%   (8 types), graphrag 3.1.0; both manifests record extraction_fingerprint 825dbb84...
%   The apd graph is the shipped gptoss@0 build (stats.json md5 2b9ce4fe... , identical to the
%   archived baseline), so its column equals the gptoss@0 column of
%   Table~\ref{tab:graph-global-constant}.
% Source: communities.parquet / community_reports.parquet / entities.parquet / text_units.parquet
%   under ~/.groundly/apd/graph/ and ~/.groundly/passc/graph/, replaying the production citation
%   join of groundly/retrieval/graph.py GraphGlobalRetriever offline.
% JOIN KEY: the `community` column, NOT community_reports.id (a content hash). graphrag's
%   read_indexer_entities sets entity.community_ids to str(int(community)), so joining on the
%   report id yields the empty set. Ceiling is COMMUNITY_LEVEL = 2, a ceiling not an equality
%   (df[df.level <= 2]). Chunk ids resolve via text_units.document_id, which ingestion/graph.py
%   sets to str(chunk_id); text-unit ids are content hashes and collide on byte-identical text
%   (apd 18 of 1193, passc 2 of 341), so the lookup is a LIST per id, exactly as the production
%   code does it -- an index lookup returns a Series there and int() raises.
% NOT DIRECTLY MEASURED -- marked est on both subjects. The replay is deterministic given the
%   parquet, but no live graph-global eval run exists on either subject: the arm calls graphrag's
%   global_search and therefore requires a provider, which is why both sweeps ran vector +
%   hybrid-local only. The replay assumes global search admits every report at level<=2 into
%   context, which is what dynamic_community_selection=False does (map-reduce over all reports at
%   the level). A live confirmation needs a provider-backed single-arm run on each subject.
% UNRANKED ARM: GraphGlobalRetriever returns sorted(resolved), i.e. ascending SQLite rowid, and
%   graph-global is in retrieval.arms.UNRANKED_ARMS. Its MRR would measure ingestion order, so it is
%   withheld rather than reported, and the arm is excluded from matched cutoffs and from
%   significance testing entirely.
% SEPARATE FILE, NOT A MERGE. tab-graph-global-constant.tex is the three-build apd series
%   (gemma@0 / gptoss@0 / gptoss@1) at a fixed corpus and is left untouched: folding passc into it
%   would present a different corpus as a fourth build condition. This table is the cross-subject
%   cut and repeats the apd gptoss@0 figures so it reads standalone.
\begin{table}[htbp]
  \centering
  \caption{The \texttt{graph-global} arm as a corpus constant on both pilot subjects, each on its
    own \texttt{gpt-oss-120b} \texttt{gleanings}~$=0$ graph. Because global search admits every
    community report under the level ceiling, the arm's citation set does not depend on the query:
    it returns the same 96.2\% of apd and the same 88.6\% of passc for every question asked. The
    behaviour survives the corpus change, so it is a property of the arm rather than of one graph
    --- an arm that returns nine tenths of the corpus in ingestion order is not performing
    retrieval.}
  \label{tab:graph-global-two-subjects}
  \begin{tabular}{lrr}
    \toprule
    Quantity & apd & passc \\
    \midrule
    Corpus chunks                                          &   1193 &    341 \\
    Community reports at level $\le 2$                     &    677 &    315 \\
    Entities under those communities\textsuperscript{est}  &   2861 &   1199 \\
    Distinct text units reached\textsuperscript{est}       &   1132 &    300 \\
    \midrule
    Chunks returned\textsuperscript{est}                   &   1148 &    302 \\
    Share of corpus\textsuperscript{est}                   & 96.2\% & 88.6\% \\
    \midrule
    MRR & -- & -- \\
    \bottomrule
  \end{tabular}

  \vspace{0.5em}
  \footnotesize
  \textsuperscript{est} Derived offline by replaying the production citation join
  (\texttt{retrieval/graph.py}, \texttt{GraphGlobalRetriever}) against each subject's parquet
  output, not observed in a live eval run. The replay is deterministic given the parquet; it
  assumes global search admits every report at level $\le 2$ into context, which is the behaviour
  of \texttt{dynamic\_community\_selection=False}. Confirming it against the live arm requires a
  provider-backed single-arm run on each subject, which was not performed --- both sweeps behind
  Table~\ref{tab:retrieval-quality-two-subjects} were provider-free and ran \texttt{vector} and
  \texttt{hybrid-local} only.
  MRR is withheld, not missing: the arm emits \texttt{sorted(chunk\_ids)}, i.e.\ ascending
  ingestion order, and is listed in \texttt{agents.ask.UNRANKED\_ARMS}, so a rank metric computed
  over it would measure ingestion order rather than relevance. The apd column is the shipped
  \texttt{gptoss@0} build and matches the corresponding column of
  Table~\ref{tab:graph-global-constant}, where the same quantity is tracked across apd's three
  graph builds and is shown to \emph{grow} as the graph improves (1138~$\rightarrow$~1148~$\rightarrow$~1168).
\end{table}
